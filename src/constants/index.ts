export const PATHS = {
  HOME: "/",
  FACILITIES: "/facilities",
  ROOMS: "/rooms",
  CONTACT: "/contact",
} as const;

export const ROUTES = {
  HOME: PATHS.HOME,
  FACILITIES: PATHS.FACILITIES,
  ROOMS: PATHS.ROOMS,
  CONTACT: PATHS.CONTACT,
} as const;
